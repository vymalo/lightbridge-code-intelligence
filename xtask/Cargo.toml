[package]
name = "xtask"
version = "0.0.0"
edition.workspace = true
license.workspace = true
publish = false
description = "Workspace automation (cargo-xtask pattern), invoked from the justfile."

[dependencies]
anyhow.workspace = true
