/** Tailwind CSS v4 via its PostCSS plugin (no tailwind.config.js — config is CSS-first in globals.css). */
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
