# Web app (apps/web)
BETTER_AUTH_SECRET=dev-only-insecure-secret-change-me
BETTER_AUTH_URL=http://localhost:3000

# Standalone Rust auth backend (services/control-plane). authN target for the rust-backend plugin.
AUTH_BACKEND_URL=http://localhost:8080
